name: Fisrt Workflow
on: workflow_dispatch
jobs:
 first-job:
  runs-on: windows-latest
  steps:
   - name: Print greeting
     run: "Hello Roro!"
   - name: Print goodbye
     run: echo "Done-byeee!"
     
